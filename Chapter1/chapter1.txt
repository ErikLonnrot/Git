Learning Git
A hands-On and Visual Guide to the Basics of Git (Anna Skoulikari)

1) Git and the Command Line
  
What is Git?
  .Git es una tecnología que permite realizar los seguimientos de los cambios de un 
   proyecto.
  .Ayuda a multiples personas a colaborar en un proyecto.
  .Git es un sistema de control de versiones(version control system).
  .Creado por Linus Torvalds (the Linux kernel).
  .Diseñado originalmente para usarse en la linea de comandos(Command Line).

Example Book Project 1-1
  .Imagina que estas escribiendo un libro y quieres usar Git para controlar todos los
   archivos del proyecto Libro.Supongamos que quieres guardar los combios del libro
   los días Lunes,Miercoles y Viernes y deseas guardar una versión para cada uno de estos
   días.Significa que tendras almenos 3 versiones del libro(A version of a project in 
   Git is called commit).Git me permite comparar las versiones y que cambios se
   realizaron entre estas versiones.
  .Supongamos que decidimos trabajar en mi proyecto con mi coauthor.Git me permite 
   trabajar en el mismo proyecto y en el mismo tiempo además de poder combinar nuestro
   trabajo cuando este listo.Por ejemplo si yo trabajo en el capítulo 1 y mi coauthor en 
   el capítulo 2,cuanto este listo combinar nuestro trabajo.
  .Si contratamos a un editor que revise el libro,también podra editar todos los capítulos
   que hemos escrito,y podremos integrar esos cambios en la versión principal del libro.
   Esto demuestra la utilidad de Git como herramienta de colaboración.
  
The Graphical User Interface and the Command Line
  .Existen dos caminos para interactuar con una computadora:
    1)The Graphical User Interface(GUI):
      .Conjunto de representaciones gráficas de objetos(icons,buttons,etc).
      .You can think of it as the point-and-click interface.
    
    2)The command line
      .Conocido también como Command Line Interface(CLI),terminal or shell.
      .A place where you can type text-based commands o interact with your computer.
 
Opening a Command Line Window
  .Debes abrir una ventana de linea de comando usando una aplicación de linea de
   comandos.En cualquier momento dado,en una ventana de línea de comando,usted se 
   encuentra en un directorio especifico,al que denominaremos 
   directorio actual(current directory).
  
  .When you open a command line window there will be a command prompt in the upper-left
  corner.El command prompt indica la ubicación del directorio(your current directory).
  
  .Al abrir una nueva ventana de línea de comandos,la ubicación del directorio 
   comienza en el directorio del usuario actual(also known as the home folder)
   representado por el simbolo de tilde(~).
  
  .We use a dollar sign($) at the end of the command prompt,but this is just one 
   example of how a command prompt may end.Your command prompt may end with the 
   a different character or symbol.For example:
     borges@debian:~$ 

Executing Commands in the Command Line
  .After the type a command,to execute it you have to press the Enter(Return) key.
  .The directory location will be indicated before the dollar sign.
      borges@debian:~/Desktop$ pwd
      /home/borges/Desktop

Command Output
  .Some command produce output,and some don’t.

Executing the firts command in the command line
  .The firts command you will practice executing in the command line is the git version
  command.
    $ git version
    git version 2.47.3

Command Options and Arguments
  .Options are settings that change the behavior of a command.And option follows a 
   single dash(-) or a double dash(--).
  .Arguements are values that provide information to the command.They will be denoted 
   by angle brackets(< >),indicating that these items should be replaced with the
   user-supplied values.Example:
   
   $ git commit -m "<message>"
    
    .Command: git commit
    .Option : -m
    .Argument: "<message>"

Clearing the Command Line
  .After you've entered a lot of commands,the command line windows gets quite cluttered.
  To clear the contents og the command line window,you can use the clear command.
  
  $ clear
    -> Clear the command line window
  
Working with Directories
  .When you first open the command line aplication you will start off in current user 
  directory(your home folder),indicated by the tilde(~) in the command prompt.You can
  also use the pwd command,which stands for "print working directory" to see the path
  to the current directory.
  
  $ pwd
    -> Show the path to the current directory
    



